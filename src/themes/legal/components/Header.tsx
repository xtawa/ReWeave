/** @jsx h */
import { h } from 'preact';

export function Header() {
    return (
        <nav class="hidden">
            {/* This header is hidden because the Layout handles the header for legal docs */}
        </nav>
    );
}
